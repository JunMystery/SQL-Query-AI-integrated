# Shared UI Assets

This folder stores reusable visual assets for SQLBot Desktop.

- `styles/light.qss` defines the initial bright minimalist theme.
- `icons/` contains shared SVG icons used by buttons and branding.

Keep future screens on these object names and color tokens before adding one-off styling.
